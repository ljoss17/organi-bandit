Specific dates can now be excluded from the season, so the scheduler skips them when placing games. The generated spreadsheet lists them alongside the schedule, with consecutive dates shown as a single range.
